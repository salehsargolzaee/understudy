# Warm-up: running totals

> ⚠️ **Placeholder exercise.** It exists to exercise the UI, not to teach anything.

Implement `running_total(xs)`. Given a list of numbers, return a new list where
the `i`-th element is the sum of `xs[0..i]` (inclusive).

For example:

```python
running_total([1, 2, 3])  # -> [1, 3, 6]
```

**Watch first:** the concept is introduced near the start of the lecture.
