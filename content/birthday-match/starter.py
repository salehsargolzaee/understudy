def first_birthday_match(students):
    """students: list of (name, "YYYY-MM-DD"). Return the first completed
    matching pair as (earlier_name, later_name), or None."""
    raise NotImplementedError
