# The first repeat

The lecture's birthday-match problem in one function. Implement
`first_duplicate(xs)`: return the first value that has already been seen while
walking left to right, or `None` if every value is distinct.

Keep it linear — a set, one pass.
